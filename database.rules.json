{
  "rules": {
    "restaurants": {
      ".read": "auth != null && query.orderByChild == 'adminEmail' && query.equalTo == auth.token.email",
      ".indexOn": ["adminEmail"],
      "$restaurantId": {
        ".read": true,
        ".write": "auth != null && (!data.exists() || data.child('adminEmail').val() == auth.token.email || newData.child('adminEmail').val() == auth.token.email)"
      }
    },
    "orders": {
      ".read": true,
      ".indexOn": ["restaurantId"],
      "$orderId": {
        ".write": "!data.exists() || (data.exists() && (auth != null || (newData.child('totalPrice').val() == data.child('totalPrice').val() && newData.child('items').val() == data.child('items').val() && newData.child('restaurantId').val() == data.child('restaurantId').val() && newData.child('customerName').val() == data.child('customerName').val())))"
      }
    },
    "inventory": {
      "$restaurantId": {
        ".read": "auth != null && root.child('restaurants/' + $restaurantId + '/adminEmail').val() == auth.token.email",
        ".write": "auth != null && root.child('restaurants/' + $restaurantId + '/adminEmail').val() == auth.token.email"
      }
    },
    "restaurant_counters": {
      "$restaurantId": {
        ".read": true,
        ".write": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 1000"
      }
    }
  }
}
